<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Pokedex</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <!-- Tailwind CDN -->
  <script src="https://cdn.tailwindcss.com"></script>
  <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/toastr.js/latest/toastr.min.css">
  
  <style>
    /* Asegurar que el body ocupe toda la altura */
    html, body {
      height: 100%;
      margin: 0;
    }
    
    body {
      display: flex;
      flex-direction: column;
    }
    
    /* El contenido principal debe crecer para empujar el footer */
    main {
      flex: 1 0 auto;
    }
    
    /* El footer siempre queda al fondo */
    footer {
      flex-shrink: 0;
      margin-top: auto;
    }
  </style>
</head>

<body>
  <!-- Tu contenido va aquí -->
  <main>
    <!-- Contenido de la página -->
  </main>

  <!-- Footer siempre al fondo -->
  <footer class="bg-red-700 py-4 sm:py-6 shadow-lg mt-auto">
    <div class="container mx-auto px-4 sm:px-6">
      <!-- Layout responsive -->
      <div class="flex flex-col sm:flex-row justify-between items-center gap-3 sm:gap-4">
        
        <!-- Logo/Título -->
        <div class="flex items-center">
          <p class="text-lg sm:text-xl md:text-2xl font-bold text-white text-center sm:text-left">
            PokeTrivia
          </p>
        </div>
        
        <!-- Navegación -->
        <nav class="flex flex-wrap justify-center sm:justify-end gap-3 sm:gap-4 md:gap-6">
          <a href="/" 
             class="text-white hover:text-yellow-300 transition-colors text-sm sm:text-base font-semibold">
            Privacidad
          </a>
          <a href="/login/loginForm" 
             class="text-white hover:text-yellow-300 transition-colors text-sm sm:text-base font-semibold">
            FAQ
          </a>
        </nav>
        
      </div>
      
      <!-- Copyright (opcional) -->
      <div class="text-center mt-3 sm:mt-4 pt-3 sm:pt-4 border-t border-red-600">
        <p class="text-white text-xs sm:text-sm opacity-80">
          © 2025 PokeTrivia. Todos los derechos reservados.
        </p>
      </div>
    </div>
  </footer>

</body>
</html>