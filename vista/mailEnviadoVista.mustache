<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Validar Email - Pokémon Trivia</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        .pokemon-border {
            border: 4px solid #2c3e50;
            box-shadow:
                inset -4px -4px 0px rgba(0,0,0,0.3),
                inset 4px 4px 0px rgba(255,255,255,0.3),
                4px 4px 0px rgba(0,0,0,0.2);
        }

        .input-pokemon {
            border: 3px solid #2c3e50;
            box-shadow: inset -2px -2px 0px rgba(0,0,0,0.2);
            font-size: 1.5rem;
            letter-spacing: 0.3rem;
            text-align: center;
            font-weight: bold;
        }

        @media (min-width: 640px) {
            .input-pokemon {
                font-size: 2rem;
                letter-spacing: 0.5rem;
            }
        }

        .input-pokemon:focus {
            outline: none;
            border-color: #3b82f6;
            box-shadow: inset -2px -2px 0px rgba(0,0,0,0.2), 0 0 0 3px rgba(59, 130, 246, 0.3);
        }
    </style>
</head>
<body class="min-h-screen bg-gradient-to-b from-sky-400 to-emerald-300 p-4">
    
    <!-- Contenedor principal centrado -->
    <div class="min-h-screen flex flex-col items-center justify-center mt-4">
        <!-- Contenedor del card -->
        <div class="w-full max-w-md mt-4">
        <!-- Card principal -->
        <div class="bg-white pokemon-border rounded-2xl p-6 sm:p-8 shadow-2xl">
            
            <!-- Icono de email -->
            <div class="flex justify-center mb-4 sm:mb-6">
                <div class="bg-gradient-to-br from-blue-400 to-blue-600 pokemon-border rounded-full p-4 sm:p-6">
                    <svg xmlns="http://www.w3.org/2000/svg" class="w-16 h-16 sm:w-20 sm:h-20 fill-white" viewBox="0 0 24 24">
                        <path d="M22 4H2v16h20zM4 18V6h16v12zM8 8H6v2h2v2h2v2h4v-2h2v-2h2V8h-2v2h-2v2h-4v-2H8z"/>
                    </svg>
                </div>
            </div>

            <!-- Título -->
            <h2 class="text-center text-gray-800 text-xl sm:text-2xl font-black mb-2">
                ¡CORREO ENVIADO!
            </h2>
            
            <!-- Descripción -->
            <div class="bg-blue-50 pokemon-border rounded-lg p-3 sm:p-4 mb-4 sm:mb-6">
                <p class="text-gray-700 text-xs sm:text-sm text-center leading-relaxed">
                    Hemos enviado un <strong>código de 6 dígitos</strong> a tu correo electrónico.
                    <br>
                    Por favor revisa tu bandeja de entrada.
                </p>
            </div>

            <!-- Formulario -->
            <form action="/registrarse/validarCodigo" method="POST" class="space-y-4 sm:space-y-6">
                <input type="hidden" name="email" value="{{email}}">
                
                <!-- Input del código -->
                <div>
                    <label for="token" class="block text-gray-700 font-bold text-xs sm:text-sm mb-2 sm:mb-3 text-center">
                        INGRESA EL CÓDIGO:
                    </label>
                    <input 
                        type="text" 
                        id="token" 
                        name="token" 
                        maxlength="6" 
                        required
                        pattern="[0-9]{6}"
                        placeholder="000000"
                        class="input-pokemon w-full py-3 sm:py-4 bg-gray-50 text-gray-800 rounded-lg"
                        autocomplete="off"
                    >
                    <p class="text-gray-500 text-xs text-center mt-2">
                        El código expira en 30 minutos
                    </p>
                </div>

                <!-- Mensaje de error -->
                {{#error}}
                <div class="bg-red-100 pokemon-border rounded-lg p-3 sm:p-4 flex items-start gap-2 sm:gap-3">
                    <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5 sm:w-6 sm:h-6 fill-red-600 flex-shrink-0 mt-0.5" viewBox="0 0 24 24">
                        <path d="M23 7v10h-1v2h-1v2h-2v1h-2v1H7v-1H5v-1H3v-2H2v-2H1V7h1V5h1V3h2V2h2V1h10v1h2v1h2v2h1v2h1M8 8v1H7v1H6v4h1v1h1v1h8v-1h1v-1h1v-4h-1V9h-1V8H8m1 2h1v1h1v2h-1v1H9v-1H8v-2h1v-1m5 0h1v1h1v2h-1v1h-1v-1h-1v-2h1v-1z"/>
                    </svg>
                    <p class="text-red-700 text-xs sm:text-sm font-bold">{{error}}</p>
                </div>
                {{/error}}

                <!-- Mensaje de éxito -->
                {{#success}}
                <div class="bg-green-100 pokemon-border rounded-lg p-3 sm:p-4 flex items-start gap-2 sm:gap-3">
                    <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5 sm:w-6 sm:h-6 fill-green-600 flex-shrink-0 mt-0.5" viewBox="0 0 24 24">
                        <path d="M22 2v20h-1v1H3v-1H2V2h1V1h18v1h1M5 11h1v-1h1V9h1v1h1v1h1v1h2v-1h1v-1h1v-1h1V8h1V7h1v1h1v1h1v1h-1v1h-1v1h-1v1h-1v1h-1v1h-1v1h-1v1h-2v-1H9v-1H8v-1H7v-1H6v-1H5v-1Z"/>
                    </svg>
                    <p class="text-green-700 text-xs sm:text-sm font-bold">{{success}}</p>
                </div>
                {{/success}}

                <!-- Botón validar -->
                <button 
                    type="submit"
                    class="w-full bg-gradient-to-b from-green-500 to-green-700 hover:from-green-600 hover:to-green-800 text-white font-black text-base sm:text-lg py-3 sm:py-4 rounded-lg pokemon-border hover:scale-105 transition-all active:scale-95">
                    VALIDAR CÓDIGO
                </button>
            </form>

            <!-- Ayuda -->
            <div class="mt-4 sm:mt-6 text-center">
                <p class="text-gray-600 text-xs">
                    ¿No recibiste el correo? Revisa tu carpeta de spam
                </p>
            </div>

        </div>

        <!-- Link volver -->
        <div class="text-center mt-4 sm:mt-6">
            <a href="/registrarse/registrarseForm" class="text-white hover:text-yellow-300 text-xs sm:text-sm font-bold transition-colors drop-shadow-lg">
                ← Volver al registro
            </a>
        </div>
        
        </div>
        <!-- Fin contenedor principal -->
    </div>

</body>
</html>