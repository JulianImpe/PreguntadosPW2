<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lobby - Pokémon Trivia</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="min-h-screen bg-gradient-to-br from-black via-gray-900 to-red-900">

<!-- Header con info del usuario -->
<header class="bg-black bg-opacity-50 backdrop-blur-sm border-b-2 border-red-700 sticky top-0 z-50">
    <div class="container mx-auto px-5 py-4 flex justify-between items-center">
        <div class="flex items-center gap-4">
            <div class="text-4xl">⚫</div>
            <div>
                <h1 class="text-2xl font-bold text-red-600 uppercase tracking-wider">Pokémon Trivia</h1>
                <p class="text-gray-400 text-sm">Bienvenido, {{usuario}}</p>
            </div>
        </div>

        <div class="flex items-center gap-4">
            <div class="hidden md:flex items-center gap-3 bg-gray-800 px-4 py-2 rounded-lg">
                <span class="text-yellow-500 text-xl">🏆</span>
                <div>
                    <p class="text-gray-400 text-xs">Puntos</p>
                    <p class="text-white font-bold">{{puntos}}</p>
                </div>
            </div>

            <a href="/PreguntadosPW2/index.php?controller=Login&method=logout"
               class="bg-red-700 hover:bg-red-600 text-white px-4 py-2 rounded-lg transition-all duration-300 font-semibold">
                Salir
            </a>
        </div>
    </div>
</header>

<!-- Contenido principal -->
<main class="container mx-auto px-5 py-10">

    <!-- Estadísticas del jugador -->
    <div class="grid grid-cols-1 md:grid-cols-4 gap-5 mb-10">
        <div class="bg-white bg-opacity-10 backdrop-blur-md rounded-2xl p-6 border-2 border-gray-700 hover:border-red-600 transition-all duration-300">
            <div class="text-4xl mb-3">🎮</div>
            <p class="text-gray-400 text-sm">Partidas Jugadas</p>
            <p class="text-white text-3xl font-bold">{{partidas_jugadas}}</p>
        </div>

        <div class="bg-white bg-opacity-10 backdrop-blur-md rounded-2xl p-6 border-2 border-gray-700 hover:border-green-600 transition-all duration-300">
            <div class="text-4xl mb-3">✅</div>
            <p class="text-gray-400 text-sm">Partidas Ganadas</p>
            <p class="text-green-400 text-3xl font-bold">{{partidas_ganadas}}</p>
        </div>

        <div class="bg-white bg-opacity-10 backdrop-blur-md rounded-2xl p-6 border-2 border-gray-700 hover:border-blue-600 transition-all duration-300">
            <div class="text-4xl mb-3">📊</div>
            <p class="text-gray-400 text-sm">Nivel</p>
            <p class="text-blue-400 text-3xl font-bold">{{nivel}}</p>
        </div>

        <div class="bg-white bg-opacity-10 backdrop-blur-md rounded-2xl p-6 border-2 border-gray-700 hover:border-yellow-600 transition-all duration-300">
            <div class="text-4xl mb-3">🏆</div>
            <p class="text-gray-400 text-sm">Ranking</p>
            <p class="text-yellow-400 text-3xl font-bold">#{{ranking}}</p>
        </div>
    </div>

    <!-- Opciones principales -->
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 mb-10">

        <!-- Jugar ahora -->
        <div class="bg-gradient-to-br from-red-700 to-red-900 rounded-3xl p-8 shadow-2xl hover:shadow-red-900/50 transition-all duration-300 hover:-translate-y-2 cursor-pointer group">
            <div class="text-6xl mb-4 group-hover:scale-110 transition-transform duration-300">🎯</div>
            <h2 class="text-3xl font-bold text-white mb-3 uppercase tracking-wider">Jugar Ahora</h2>
            <p class="text-gray-200 mb-6">Inicia una nueva partida y demuestra tus conocimientos</p>
            <a href="/PreguntadosPW2/index.php?controller=Juego&method=iniciar"
               class="inline-block bg-white text-red-700 px-6 py-3 rounded-lg font-bold hover:bg-gray-100 transition-all duration-300">
                Comenzar Partida
            </a>
        </div>

        <!-- Ranking -->
        <div class="bg-gradient-to-br from-yellow-600 to-yellow-800 rounded-3xl p-8 shadow-2xl hover:shadow-yellow-900/50 transition-all duration-300 hover:-translate-y-2 cursor-pointer group">
            <div class="text-6xl mb-4 group-hover:scale-110 transition-transform duration-300">🏆</div>
            <h2 class="text-3xl font-bold text-white mb-3 uppercase tracking-wider">Ranking</h2>
            <p class="text-gray-100 mb-6">Mira dónde te encuentras entre los mejores</p>
            <a href="/PreguntadosPW2/index.php?controller=Ranking&method=ver"
               class="inline-block bg-white text-yellow-700 px-6 py-3 rounded-lg font-bold hover:bg-gray-100 transition-all duration-300">
                Ver Ranking
            </a>
        </div>

        <!-- Perfil -->
        <div class="bg-gradient-to-br from-blue-700 to-blue-900 rounded-3xl p-8 shadow-2xl hover:shadow-blue-900/50 transition-all duration-300 hover:-translate-y-2 cursor-pointer group">
            <div class="text-6xl mb-4 group-hover:scale-110 transition-transform duration-300">👤</div>
            <h2 class="text-3xl font-bold text-white mb-3 uppercase tracking-wider">Mi Perfil</h2>
            <p class="text-gray-100 mb-6">Revisa y edita tu información personal</p>
            <a href="/PreguntadosPW2/index.php?controller=Perfil&method=ver"
               class="inline-block bg-white text-blue-700 px-6 py-3 rounded-lg font-bold hover:bg-gray-100 transition-all duration-300">
                Ver Perfil
            </a>
        </div>

    </div>

    <!-- Historial reciente -->
    <div class="bg-white bg-opacity-10 backdrop-blur-md rounded-3xl p-8 border-2 border-gray-700">
        <h2 class="text-2xl font-bold text-white mb-6 flex items-center gap-3">
            <span class="text-3xl">📜</span>
            Últimas Partidas
        </h2>

        {{#partidas_recientes}}
            <div class="bg-gray-800 bg-opacity-50 rounded-xl p-4 mb-3 hover:bg-opacity-70 transition-all duration-300 flex justify-between items-center">
                <div>
                    <p class="text-white font-semibold">{{fecha}}</p>
                    <p class="text-gray-400 text-sm">Puntos: {{puntos_obtenidos}} | Preguntas correctas: {{correctas}}/{{totales}}</p>
                </div>
                <div class="text-2xl">
                    {{#ganada}}✅{{/ganada}}
                    {{^ganada}}❌{{/ganada}}
                </div>
            </div>
        {{/partidas_recientes}}

        {{^partidas_recientes}}
            <p class="text-gray-400 text-center py-8">
                Aún no has jugado ninguna partida. ¡Es hora de empezar! 🎮
            </p>
        {{/partidas_recientes}}
    </div>

</main>

<!-- Footer -->
<footer class="bg-black bg-opacity-50 mt-20 py-6 border-t-2 border-red-700">
    <div class="container mx-auto px-5 text-center">
        <p class="text-gray-400">© 2025 Pokémon Trivia - Todos los derechos reservados</p>
    </div>
</footer>

</body>
</html>