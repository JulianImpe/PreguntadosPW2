<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lobby - Pokémon Trivia</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Press+Start+2P&display=swap');

        .pixel-font {
            font-family: 'Press Start 2P', cursive;
        }

        .pokemon-border {
            border: 4px solid #2c3e50;
            box-shadow:
                    inset -4px -4px 0px rgba(0,0,0,0.3),
                    inset 4px 4px 0px rgba(255,255,255,0.3),
                    4px 4px 0px rgba(0,0,0,0.2);
        }

        .pokeball-bg {
            background: linear-gradient(180deg, #ef4444 0%, #ef4444 45%, #1f2937 45%, #1f2937 55%, #f3f4f6 55%, #f3f4f6 100%);
        }
    </style>
</head>
<body class="min-h-screen bg-gradient-to-b from-sky-400 to-emerald-300">

<!-- Header estilo Game Boy -->
<header class="bg-red-600 border-b-8 border-yellow-400 shadow-2xl sticky top-0 z-50">
    <div class="container mx-auto px-5 py-3">
        <div class="flex justify-between items-center">
            <div class="flex items-center gap-3">
                <div class="w-12 h-12 pokeball-bg rounded-full border-4 border-gray-800"></div>
                <div>
                    <h1 class="pixel-font text-yellow-300 text-xl drop-shadow-lg" style="text-shadow: 3px 3px 0px #b45309;">POKÉMON</h1>
                    <p class="text-white text-xs font-bold">Entrenador: {{usuario}}</p>
                </div>
            </div>

            <div class="flex items-center gap-3">
                <div class="hidden md:flex items-center gap-2 bg-yellow-400 pokemon-border px-4 py-2 rounded-lg">
                    <span class="text-xl">⭐</span>
                    <p class="font-black text-gray-800">{{puntos}}</p>
                </div>

                <a href="/PreguntadosPW2/index.php?controller=Login&method=logout"
                   class="bg-gray-800 hover:bg-gray-700 text-white px-4 py-2 rounded pokemon-border font-bold transition-all">
                    SALIR
                </a>
            </div>
        </div>
    </div>
</header>

<!-- Contenido principal -->
<main class="container mx-auto px-5 py-8">

    <!-- Medallas/Stats -->
    <div class="grid grid-cols-2 md:grid-cols-4 gap-4 mb-8">
        <div class="bg-white pokemon-border rounded-xl p-4 text-center hover:-translate-y-1 transition-transform">
            <div class="text-4xl mb-2">🎮</div>
            <p class="text-gray-600 text-xs font-bold">PARTIDAS</p>
            <p class="text-gray-800 text-2xl font-black">{{partidas_jugadas}}</p>
        </div>

        <div class="bg-gradient-to-br from-yellow-300 to-yellow-500 pokemon-border rounded-xl p-4 text-center hover:-translate-y-1 transition-transform">
            <div class="text-4xl mb-2">🏆</div>
            <p class="text-yellow-900 text-xs font-bold">VICTORIAS</p>
            <p class="text-yellow-900 text-2xl font-black">{{partidas_ganadas}}</p>
        </div>

        <div class="bg-gradient-to-br from-blue-400 to-blue-600 pokemon-border rounded-xl p-4 text-center hover:-translate-y-1 transition-transform">
            <div class="text-4xl mb-2">⚡</div>
            <p class="text-blue-100 text-xs font-bold">NIVEL</p>
            <p class="text-white text-2xl font-black">{{nivel}}</p>
        </div>

        <div class="bg-gradient-to-br from-orange-400 to-red-500 pokemon-border rounded-xl p-4 text-center hover:-translate-y-1 transition-transform">
            <div class="text-4xl mb-2">🥇</div>
            <p class="text-red-100 text-xs font-bold">RANKING</p>
            <p class="text-white text-2xl font-black">#{{ranking}}</p>
        </div>
    </div>
    <div class="mb-8 text-center">
        <form method="GET" action="/PreguntadosPW2/index.php" id="crearPartidaForm">
            <input type="hidden" name="controller" value="Lobby">
            <input type="hidden" name="method" value="crearPartidaVista">
            <button type="submit"
                    class="inline-block bg-gradient-to-b from-red-500 to-red-700 hover:from-red-600 hover:to-red-800 text-white pixel-font text-lg px-8 py-6 rounded-full pokemon-border hover:scale-105 transition-all">
                <div class="flex items-center gap-3">
                    <span class="text-3xl">⚫</span>
                    <span>¡COMBATIR!</span>
                </div>
            </button>
        </form>
    </div>
    <!-- Menú principal - estilo Pokédex -->
    <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-8">

        <!-- Ranking -->
        <div class="bg-gradient-to-br from-yellow-300 to-yellow-500 pokemon-border rounded-2xl p-6 hover:-translate-y-1 transition-all cursor-pointer">
            <div class="flex items-center gap-4 mb-4">
                <div class="w-16 h-16 bg-white rounded-full flex items-center justify-center pokemon-border">
                    <span class="text-4xl">🏆</span>
                </div>
                <div>
                    <h2 class="pixel-font text-yellow-900 text-sm mb-1">LIGA</h2>
                    <p class="text-yellow-800 text-xs">Tabla de clasificación</p>
                </div>
            </div>
            <a href="/PreguntadosPW2/index.php?controller=Ranking&method=ver"
               class="block bg-yellow-900 hover:bg-yellow-800 text-white text-center px-6 py-3 rounded-lg font-bold transition-all">
                VER ENTRENADORES
            </a>
        </div>

        <!-- Perfil -->
        <div class="bg-gradient-to-br from-blue-400 to-blue-600 pokemon-border rounded-2xl p-6 hover:-translate-y-1 transition-all cursor-pointer">
            <div class="flex items-center gap-4 mb-4">
                <div class="w-16 h-16 bg-white rounded-full flex items-center justify-center pokemon-border">
                    <span class="text-4xl">👤</span>
                </div>
                <div>
                    <h2 class="pixel-font text-blue-100 text-sm mb-1">PERFIL</h2>
                    <p class="text-blue-200 text-xs">Datos del entrenador</p>
                </div>
            </div>
            <a href="/PreguntadosPW2/index.php?controller=Perfil&method=ver"
               class="block bg-blue-900 hover:bg-blue-800 text-white text-center px-6 py-3 rounded-lg font-bold transition-all">
                VER MI PERFIL
            </a>
        </div>

    </div>

    <!-- Historial - estilo diálogo Pokémon -->
    <div class="bg-white pokemon-border rounded-2xl p-6">
        <div class="flex items-center gap-3 mb-5 pb-4 border-b-4 border-gray-300">
            <span class="text-3xl">📋</span>
            <h2 class="pixel-font text-gray-800 text-sm">HISTORIAL</h2>
        </div>

        {{#partidas_recientes}}
            <div class="bg-gradient-to-r from-gray-100 to-gray-200 pokemon-border rounded-xl p-4 mb-3 hover:from-gray-200 hover:to-gray-300 transition-all flex justify-between items-center">
                <div>
                    <p class="text-gray-800 font-bold text-sm">{{fecha}}</p>
                    <p class="text-gray-600 text-xs mt-1">
                        <span class="inline-block bg-yellow-400 px-2 py-1 rounded font-bold mr-2">{{puntos_obtenidos}} PTS</span>
                        <span>{{correctas}}/{{totales}} correctas</span>
                    </p>
                </div>
                <div class="text-3xl">
                    {{#ganada}}✅{{/ganada}}
                    {{^ganada}}❌{{/ganada}}
                </div>
            </div>
        {{/partidas_recientes}}

        {{^partidas_recientes}}
            <div class="text-center py-8 bg-gray-100 rounded-xl">
                <p class="text-gray-500 font-bold mb-2">¡No hay batallas registradas!</p>
                <p class="text-5xl mb-3">🎮</p>
                <p class="text-gray-400 text-sm">Empieza tu aventura ahora</p>
            </div>
        {{/partidas_recientes}}
    </div>

</main>



<!-- Footer -->
<footer class="bg-red-600 border-t-8 border-yellow-400 mt-12 py-6">
    <div class="container mx-auto px-5 text-center">
        <p class="text-white font-bold text-sm">⚫ POKÉMON TRIVIA © 2025 ⚫</p>
        <p class="text-yellow-300 text-xs mt-1">¡Hazte con todos!</p>
    </div>
</footer>

</body>
</html>